# 服务商配置目录

本目录包含 Claude Code Proxy 的服务商配置文件。

## 📁 文件说明

### providers.json

**用途：**
- ✅ 配置管理页面的预设服务商
- ✅ 推荐服务页面的展示列表
- ✅ 统一的服务商信息管理

**特点：**
- 单一数据源，避免配置冗余
- 支持完整的服务商信息
- 可热重载（无需重启应用）

### recommendations.json

**状态：** 已弃用（保留用于向后兼容）

**说明：**
- 旧版本的推荐服务配置格式
- 现已整合到 `providers.json`
- 系统优先读取 `providers.json`

---

## 🚀 快速开始

### 方式一：查看完整教程

推荐阅读详细教程文档：

```
项目根目录/推荐服务配置教程.md
```

该文档包含：
- 完整的字段说明
- 详细的配置示例
- 操作步骤指南
- 常见问题解答
- 最佳实践建议

### 方式二：快速配置

1. **打开配置文件**
   ```bash
   # 使用你喜欢的编辑器
   code config/providers.json
   vim config/providers.json
   ```

2. **添加新服务**
   ```json
   {
     "id": "my-service",
     "name": "我的服务",
     "category": "third_party",
     "websiteUrl": "https://my-service.com",
     "serverUrl": "https://api.my-service.com",
     "description": "服务简介",
     "showInRecommendations": true,
     "isRecommended": true,
     "hotnessScore": 85,
     "region": "domestic"
   }
   ```

3. **刷新应用**

   在应用的"推荐服务"页面点击"刷新"按钮

---

## 📖 配置格式

### 文件结构

```json
{
  "version": "1.0",
  "description": "Claude API 服务商配置",
  "providers": [
    {
      // 服务商配置...
    }
  ]
}
```

### 必填字段

| 字段 | 类型 | 说明 | 示例 |
|------|------|------|------|
| `id` | String | 唯一标识符 | `"claude-official"` |
| `name` | String | 服务商名称 | `"Claude Official"` |
| `category` | String | 服务商分类 | `"official"` |
| `websiteUrl` | String | 官网地址 | `"https://anthropic.com"` |
| `serverUrl` | String | API 服务器地址 | `"https://api.anthropic.com"` |

### 推荐服务专用字段

| 字段 | 类型 | 默认值 | 说明 |
|------|------|--------|------|
| `description` | String | `""` | 服务商简介（显示在卡片上） |
| `showInRecommendations` | Boolean | `true` | 是否在推荐页面显示 |
| `isRecommended` | Boolean | `false` | 是否显示"推荐"标签 |
| `hotnessScore` | Number | `0` | 热度评分（0-100，用于排序） |
| `region` | String | `"domestic"` | 服务区域（`domestic`/`international`） |

### 完整字段列表

更多字段说明，请参考：
- [推荐服务配置教程.md](../推荐服务配置教程.md) - 完整的字段文档
- [providers.json](providers.json) - 实际配置示例

---

## 🎯 服务商分类

### category 字段值

| 值 | 中文名称 | 适用场景 |
|----|----------|----------|
| `official` | 官方 | Anthropic Claude Official |
| `cn_official` | 国产官方 | DeepSeek, GLM, Qwen, Kimi |
| `aggregator` | 聚合平台 | AiHubMix, ModelScope |
| `third_party` | 第三方供应商 | PackyCode, AnyRouter |
| `custom` | 自定义 | 用户自建服务 |

### region 字段值

| 值 | 显示标签 | 标签颜色 | 说明 |
|----|----------|----------|------|
| `domestic` | 国内 | 🟢 绿色 | 国内可直连 |
| `international` | 国外 | 🔵 蓝色 | 需科学上网 |

---

## 💡 配置示例

### 示例 1：简单推荐服务

```json
{
  "id": "simple-api",
  "name": "简单 API",
  "category": "third_party",
  "websiteUrl": "https://simple-api.com",
  "serverUrl": "https://api.simple-api.com",
  "description": "简单易用的 Claude API 服务",
  "showInRecommendations": true,
  "isRecommended": true,
  "hotnessScore": 80,
  "region": "domestic"
}
```

### 示例 2：完整配置

```json
{
  "id": "complete-api",
  "name": "完整 API",
  "category": "aggregator",
  "websiteUrl": "https://complete-api.com",
  "apiKeyUrl": "https://complete-api.com/keys",
  "serverUrl": "https://api.complete-api.com",
  "description": "企业级 API 服务，支持多模型",
  "showInRecommendations": true,
  "isRecommended": true,
  "isPartner": true,
  "hotnessScore": 95,
  "region": "domestic",
  "defaultModel": "claude-sonnet-4-5-20250929",
  "haikuModel": "claude-haiku-4-5-20251001",
  "sonnetModel": "claude-sonnet-4-5-20250929",
  "opusModel": "claude-opus-4-20250514",
  "apiTimeoutMs": 600000,
  "maxOutputTokens": 65000,
  "endpointCandidates": [
    "https://api.complete-api.com",
    "https://backup.complete-api.com"
  ]
}
```

---

## 🛠 常用操作

### 添加新服务

1. 在 `providers` 数组末尾添加新对象
2. 确保 `id` 唯一
3. 填写必填字段
4. 保存文件
5. 在应用中刷新

### 修改服务

1. 找到对应的服务（通过 `id` 查找）
2. 修改需要更新的字段
3. 保存文件
4. 刷新应用

### 删除服务

1. 删除整个服务对象
2. 注意处理 JSON 逗号
3. 保存文件
4. 刷新应用

### 调整排序

修改 `hotnessScore` 字段：
- 数值越大，排序越靠前
- 推荐范围：50-100

---

## ⚠️ 注意事项

### JSON 格式要求

1. **使用双引号** - 字符串必须用双引号
2. **注意逗号** - 最后一项不要逗号
3. **布尔值小写** - `true`/`false` 不加引号
4. **数字不加引号** - 如 `"hotnessScore": 95`

### 常见错误

❌ **错误示例**：
```json
{
  'id': 'test',        // 错误：不能用单引号
  "name": "Test",      // 错误：缺少逗号
  "score": "95"        // 错误：数字不应加引号
  "enabled": True      // 错误：布尔值应小写
}
```

✅ **正确示例**：
```json
{
  "id": "test",
  "name": "Test",
  "score": 95,
  "enabled": true
}
```

### 验证工具

推荐使用以下工具验证 JSON 格式：
- [JSONLint](https://jsonlint.com/) - 在线验证
- VS Code - 自动语法检查
- JetBrains IDE - 内置 JSON 验证

---

## 🔧 故障排除

### 配置不生效

**可能原因：**
1. JSON 格式错误
2. 未点击刷新按钮
3. 文件路径错误

**解决方案：**
1. 使用工具验证 JSON 格式
2. 在应用中点击"刷新"
3. 检查文件是否在 `config/providers.json`

### 服务不显示

**检查清单：**
- [ ] `isRecommended` 设为 `true`
- [ ] 必填字段完整
- [ ] JSON 格式正确
- [ ] 已刷新应用

### 显示异常

**常见问题：**
- 简介过长 → 控制在 100 字符内
- 排序错误 → 检查 `hotnessScore`
- 标签错误 → 检查 `region` 拼写

---

## 📚 相关资源

### 文档

- [推荐服务配置教程.md](../推荐服务配置教程.md) - 详细配置指南
- [providers.json](providers.json) - 配置文件

### 应用功能

- **推荐服务页面** - 显示推荐的服务商列表
- **配置管理页面** - 快速添加预设服务商配置

---

## 📝 修改日志

### 2025-11-11 - v1.0
- ✅ 统一配置到 `providers.json`
- ✅ 添加 `region` 字段支持
- ✅ 整合推荐服务和配置管理功能
- ✅ 移除 `src-tauri/config` 目录
- ✅ 更新文档和教程

---

## 💬 支持

遇到问题？

1. 查看 [推荐服务配置教程.md](../推荐服务配置教程.md)
2. 检查应用日志
3. 验证 JSON 格式
4. 恢复备份文件

---

**最后更新**: 2025-11-11
**维护者**: Claude Code Proxy Team
