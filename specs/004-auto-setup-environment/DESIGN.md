# Claude Code 一键环境配置功能设计

## 功能概述

为 Claude Code Proxy 添加一键自动安装和配置 Claude Code 环境的功能，支持 Windows、macOS 和 Linux 三大平台，实现 Zero-Config Code Flow 工作流。

## 核心功能

### 1. 环境依赖检测

**检测项目：**
- ✅ 操作系统类型和版本
- ✅ Claude Code CLI 是否已安装
- ✅ Node.js 版本（如果使用 NPM 安装）
- ✅ ripgrep 是否可用
- ✅ Shell 环境（Bash/Zsh/Fish）
- ✅ Windows 特定：WSL 或 Git Bash
- ✅ macOS 特定：Homebrew
- ✅ 网络连接状态

### 2. 自动安装流程

**macOS：**
```bash
# 检测并安装 Homebrew（如果未安装）
if ! command -v brew &> /dev/null; then
    /bin/bash -c "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/HEAD/install.sh)"
fi

# 安装 Claude Code
brew install --cask claude-code

# 验证安装
claude doctor
```

**Linux：**
```bash
# 使用官方安装脚本
curl -fsSL https://claude.ai/install.sh | bash

# 验证安装
claude doctor
```

**Windows：**
```powershell
# 检测 WSL
if (!(Get-Command wsl -ErrorAction SilentlyContinue)) {
    # 提示用户安装 WSL
    Write-Host "需要安装 WSL..."
}

# 使用 PowerShell 安装脚本
irm https://claude.ai/install.ps1 | iex

# 验证安装
claude doctor
```

### 3. Zero-Config Code Flow 集成

**工作流程：**
1. 用户点击"一键配置 Claude Code"
2. 系统检测当前环境
3. 显示检测结果和缺失的依赖
4. 用户确认后自动安装
5. 自动配置 Claude Code Proxy
6. 启动代理服务
7. 测试连接

**自动化配置：**
- ✅ 自动创建 Claude Code 配置
- ✅ 自动设置代理地址（localhost:25341）
- ✅ 自动备份原配置
- ✅ 自动测试连接

### 4. 智能诊断和修复

**诊断功能：**
- 运行 `claude doctor` 检测问题
- 解析诊断结果
- 提供针对性解决方案
- 一键修复常见问题

**常见问题自动修复：**
- Node.js 版本过低 → 提示升级
- ripgrep 缺失 → 自动安装
- 权限问题 → 提供解决方案
- 网络问题 → 测试连接并提示

## UI 设计

### 环境设置页面

```
┌─────────────────────────────────────────────────────────────┐
│  🔧 Claude Code 环境配置                                      │
├─────────────────────────────────────────────────────────────┤
│                                                              │
│  【系统检测】                                                 │
│  ┌──────────────────────────────────────────────────────┐   │
│  │ ✅ 操作系统: macOS 14.0 (支持)                        │   │
│  │ ✅ Shell 环境: Zsh 5.9 (推荐)                         │   │
│  │ ❌ Claude Code: 未安装                                │   │
│  │ ✅ Homebrew: 已安装 (4.2.0)                          │   │
│  │ ✅ 网络连接: 正常                                      │   │
│  │ ✅ ripgrep: 已安装 (14.1.0)                          │   │
│  └──────────────────────────────────────────────────────┘   │
│                                                              │
│  【安装选项】                                                 │
│  ┌──────────────────────────────────────────────────────┐   │
│  │ 安装方式:                                              │   │
│  │  ⦿ 原生安装 (推荐)                                     │   │
│  │  ○ NPM 安装                                            │   │
│  │                                                        │   │
│  │ 安装位置: /usr/local/bin/claude                       │   │
│  │ 配置目录: ~/.config/claude-code/                      │   │
│  └──────────────────────────────────────────────────────┘   │
│                                                              │
│  【自动配置】                                                 │
│  ┌──────────────────────────────────────────────────────┐   │
│  │ ✓ 安装后自动配置代理                                   │   │
│  │ ✓ 自动备份原有配置                                     │   │
│  │ ✓ 自动测试连接                                         │   │
│  │ ✓ 启动代理服务                                         │   │
│  └──────────────────────────────────────────────────────┘   │
│                                                              │
│  [🚀 一键安装配置]  [🔍 重新检测]  [📋 查看日志]           │
│                                                              │
│  【安装日志】                                                 │
│  ┌──────────────────────────────────────────────────────┐   │
│  │ [14:32:10] 开始检测系统环境...                        │   │
│  │ [14:32:11] ✅ 系统检测完成                            │   │
│  │ [14:32:12] 准备下载 Claude Code...                   │   │
│  │ [14:32:15] 正在安装 Claude Code...                   │   │
│  │ [14:32:20] ✅ Claude Code 安装成功                   │   │
│  │ [14:32:21] 正在配置代理...                            │   │
│  │ [14:32:22] ✅ 代理配置完成                            │   │
│  │ [14:32:23] 正在测试连接...                            │   │
│  │ [14:32:25] ✅ 所有配置完成！                          │   │
│  └──────────────────────────────────────────────────────┘   │
│                                                              │
└─────────────────────────────────────────────────────────────┘
```

### 快速启动向导

首次启动应用时显示：

```
┌─────────────────────────────────────────────────────────────┐
│  🎉 欢迎使用 Claude Code Proxy                               │
├─────────────────────────────────────────────────────────────┤
│                                                              │
│  我们检测到您的系统尚未安装 Claude Code                       │
│                                                              │
│  Claude Code Proxy 可以帮您：                                │
│  • 自动检测系统环境                                          │
│  • 一键安装 Claude Code                                      │
│  • 自动配置代理服务                                          │
│  • 立即开始使用                                               │
│                                                              │
│  [🚀 开始自动配置]  [⏭️ 跳过，稍后配置]                      │
│                                                              │
└─────────────────────────────────────────────────────────────┘
```

## 技术实现

### 后端 (Rust)

**新增模块：**

1. `src-tauri/src/services/env_detection.rs`
   - 检测操作系统信息
   - 检测已安装的工具和依赖
   - 生成环境报告

2. `src-tauri/src/services/claude_installer.rs`
   - 下载和执行安装脚本
   - 监控安装进度
   - 验证安装结果

3. `src-tauri/src/services/auto_config.rs`
   - 自动配置 Claude Code
   - 创建和备份配置文件
   - 测试连接

4. `src-tauri/src/commands/setup.rs`
   - Tauri Commands for UI

**数据模型：**

```rust
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct EnvironmentStatus {
    pub os_type: String,
    pub os_version: String,
    pub shell: Option<String>,
    pub claude_installed: bool,
    pub claude_version: Option<String>,
    pub homebrew_installed: bool,  // macOS
    pub wsl_installed: bool,       // Windows
    pub git_bash_installed: bool,  // Windows
    pub node_installed: bool,
    pub node_version: Option<String>,
    pub ripgrep_installed: bool,
    pub network_available: bool,
}

#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct InstallOptions {
    pub method: InstallMethod,  // Native or NPM
    pub auto_configure: bool,
    pub auto_backup: bool,
    pub auto_test: bool,
    pub auto_start_proxy: bool,
}

#[derive(Debug, Clone, Serialize, Deserialize)]
pub enum InstallMethod {
    Native,
    NPM,
}

#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct InstallProgress {
    pub stage: InstallStage,
    pub progress: f32,  // 0.0 - 1.0
    pub message: String,
    pub success: bool,
}

#[derive(Debug, Clone, Serialize, Deserialize)]
pub enum InstallStage {
    Detecting,
    Downloading,
    Installing,
    Configuring,
    Testing,
    Complete,
    Failed,
}
```

### 前端 (React + TypeScript)

**新增页面：**

1. `src-ui/src/pages/EnvironmentSetup.tsx`
   - 环境设置主页面
   - 显示检测结果
   - 安装进度显示
   - 日志输出

2. `src-ui/src/components/SetupWizard.tsx`
   - 首次启动向导
   - 引导用户完成配置

**API 调用：**

```typescript
// src-ui/src/api/setup.ts
import { invoke } from '@tauri-apps/api/tauri';

export interface EnvironmentStatus {
  os_type: string;
  os_version: string;
  shell: string | null;
  claude_installed: boolean;
  claude_version: string | null;
  homebrew_installed: boolean;
  wsl_installed: boolean;
  git_bash_installed: boolean;
  node_installed: boolean;
  node_version: string | null;
  ripgrep_installed: boolean;
  network_available: boolean;
}

export interface InstallOptions {
  method: 'Native' | 'NPM';
  auto_configure: boolean;
  auto_backup: boolean;
  auto_test: boolean;
  auto_start_proxy: boolean;
}

export async function detectEnvironment(): Promise<EnvironmentStatus> {
  return await invoke('detect_environment');
}

export async function installClaudeCode(options: InstallOptions): Promise<void> {
  return await invoke('install_claude_code', { options });
}

export async function runClaudeDoctor(): Promise<string> {
  return await invoke('run_claude_doctor');
}
```

## Zero-Config Code Flow 工作流

### 场景 1: 全新用户

1. 用户首次启动应用
2. 显示欢迎向导
3. 用户点击"开始自动配置"
4. 自动检测环境 → 安装 Claude Code → 配置代理 → 测试连接
5. 完成后直接可用

### 场景 2: 已有 Claude Code 用户

1. 用户启动应用
2. 检测到已安装 Claude Code
3. 提示是否配置代理
4. 用户确认 → 自动配置代理 → 测试连接
5. 完成后直接可用

### 场景 3: 环境诊断

1. 用户遇到问题
2. 打开环境设置页面
3. 点击"运行诊断"
4. 显示诊断结果和建议修复方案
5. 一键修复或手动处理

## 安全考虑

1. **权限控制**
   - 安装脚本需要用户明确授权
   - 不使用 sudo 安装（避免权限问题）
   - 配置文件修改前自动备份

2. **脚本验证**
   - 验证下载脚本的签名
   - 使用官方安装源
   - 超时和重试机制

3. **错误处理**
   - 详细的错误日志
   - 回滚机制
   - 用户友好的错误提示

## 实现优先级

### Phase 1: 核心检测（本周）
- ✅ 环境检测模块
- ✅ 基础 UI 页面
- ✅ macOS 支持

### Phase 2: 自动安装（下周）
- ✅ macOS 自动安装
- ✅ Linux 自动安装
- ✅ Windows 支持（WSL + Git Bash）

### Phase 3: 智能配置（第三周）
- ✅ 自动配置流程
- ✅ 一键诊断修复
- ✅ 向导优化

### Phase 4: 完善体验（第四周）
- ✅ 安装进度优化
- ✅ 错误处理完善
- ✅ 全平台测试

## 参考资料

- [Claude Code 官方文档](https://code.claude.com/docs/en/setup)
- [Claude Code 安装指南](https://claudelog.com/install-claude-code/)
- [跨平台安装最佳实践](https://blog.getbind.co/2025/08/26/how-to-install-claude-code-cli/)

## 成功指标

- ✅ 95% 以上的用户可一键完成配置
- ✅ 平均配置时间 < 3 分钟
- ✅ 支持三大主流平台
- ✅ 错误恢复率 > 90%
