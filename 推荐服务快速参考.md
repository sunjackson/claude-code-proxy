# 推荐服务快速参考

> 📌 快速查阅版 - 完整教程请看 [推荐服务配置教程.md](推荐服务配置教程.md)

## 📍 配置文件位置

```
项目根目录/config/providers.json
```

## ⚡ 快速添加服务

### 1. 最简配置（仅必填字段）

```json
{
  "id": "my-service",
  "name": "我的服务",
  "category": "third_party",
  "websiteUrl": "https://my-service.com",
  "serverUrl": "https://api.my-service.com"
}
```

### 2. 推荐服务配置（常用）

```json
{
  "id": "my-service",
  "name": "我的服务",
  "category": "third_party",
  "websiteUrl": "https://my-service.com",
  "serverUrl": "https://api.my-service.com",
  "description": "服务简介（30-80字）",
  "showInRecommendations": true,
  "isRecommended": true,
  "hotnessScore": 85,
  "region": "domestic"
}
```

### 3. 完整配置（所有字段）

```json
{
  "id": "my-service",
  "name": "我的服务",
  "category": "aggregator",
  "websiteUrl": "https://my-service.com",
  "apiKeyUrl": "https://my-service.com/api-keys",
  "serverUrl": "https://api.my-service.com",
  "description": "详细的服务商简介",
  "showInRecommendations": true,
  "isRecommended": true,
  "isPartner": false,
  "hotnessScore": 90,
  "region": "domestic",
  "defaultModel": "claude-sonnet-4-5-20250929",
  "haikuModel": "claude-haiku-4-5-20251001",
  "sonnetModel": "claude-sonnet-4-5-20250929",
  "opusModel": "claude-opus-4-20250514",
  "smallFastModel": "claude-haiku-4-5-20251001",
  "apiTimeoutMs": 600000,
  "maxOutputTokens": 65000,
  "endpointCandidates": []
}
```

## 🔑 关键字段说明

| 字段 | 取值 | 说明 |
|------|------|------|
| `category` | `official` / `cn_official` / `aggregator` / `third_party` / `custom` | 服务商分类 |
| `region` | `domestic` / `international` | 国内/国外 |
| `showInRecommendations` | `true` / `false` | 是否在推荐页显示 |
| `isRecommended` | `true` / `false` | 显示"推荐"标签 |
| `hotnessScore` | 0-100 | 数值越大越靠前 |

## 📊 热度评分建议

| 分数 | 推荐程度 |
|------|----------|
| 90-100 | 强烈推荐 ⭐⭐⭐ |
| 75-89 | 推荐使用 ⭐⭐ |
| 60-74 | 可选择 ⭐ |
| 50-59 | 一般 |

## 🛠️ 常用操作

### 添加服务
1. 打开 `config/providers.json`
2. 在 `providers` 数组末尾添加新对象
3. 保存文件
4. 在应用中点击"刷新"

### 修改服务
1. 找到对应的服务（通过 `id`）
2. 修改字段
3. 保存并刷新

### 删除服务
1. 删除整个服务对象
2. 注意处理逗号
3. 保存并刷新

### 调整排序
修改 `hotnessScore` 值（数值越大越靠前）

## ⚠️ 注意事项

- ✅ JSON 字符串必须用**双引号**
- ✅ 最后一项**不要逗号**
- ✅ 布尔值小写：`true` / `false`
- ✅ 数字不加引号：`"hotnessScore": 85`
- ✅ 每个 `id` 必须唯一

## 🔧 JSON 验证

**在线工具**: [JSONLint.com](https://jsonlint.com/)

**常见错误**:
```json
❌ 'id': 'test'          // 不能用单引号
❌ "name": "Test"        // 缺少逗号
❌ "score": "95"         // 数字不应加引号
❌ "enabled": True       // 应为小写 true

✅ "id": "test",
✅ "name": "Test",
✅ "score": 95,
✅ "enabled": true
```

## 📚 完整文档

- 📖 [推荐服务配置教程.md](推荐服务配置教程.md) - 详细教程
- 📁 [config/README.md](config/README.md) - 配置目录说明
- 📝 [config/providers.json](config/providers.json) - 实际配置

## 🆘 遇到问题？

1. 检查 JSON 格式（用 JSONLint 验证）
2. 确认已点击"刷新"按钮
3. 查看应用日志
4. 参考现有配置示例

---

**快速通道**: 想深入了解？阅读 [推荐服务配置教程.md](推荐服务配置教程.md)
